#include<stdio.h>
int main()
{
	int year;
	printf("enter the year:");
	scanf("%d",&year);
	if(year%4==0)
	{
		printf("%d is leap year.\n",year);
	}
	else
	{
		printf("%d is non leap year. ",year);
	}
	return 0;
}
